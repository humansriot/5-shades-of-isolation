apiVersion: apps/v1
kind: Deployment
metadata:
  name: fileparser-deployment
  labels:
    app: fileparser
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fileparser
  template:
    metadata:
      labels:
        app: fileparser
    spec:
      containers:
      - name: fileparser
        image: humansriot/fileparser:latest
        ports:
        - containerPort: 8100