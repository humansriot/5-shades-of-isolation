apiVersion: v1
kind: Service
metadata:
  name: {{ include "httptime-chart.fullname" . }}
  labels:
    {{- include "httptime-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "httptime-chart.selectorLabels" . | nindent 4 }}
