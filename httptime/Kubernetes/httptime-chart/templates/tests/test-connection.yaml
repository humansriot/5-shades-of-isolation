apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "httptime-chart.fullname" . }}-test-connection"
  labels:
    {{- include "httptime-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "httptime-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
