apiVersion: v1
kind: ConfigMap
metadata:
  name: httptime-config
data:
  config.yml: |
    # config.yaml

    # port to serve on
    port: 8080

    # log to stdout instead of log file
    stdout: false